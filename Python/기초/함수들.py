'''
.capitalize() # 맨첫글자만 대문자
.title() # 단어의 첫글자 대문자
.count('o') # o의 갯수
.find('o') # 첫 o의 위치(index값), 존재하지 않으면 -1 출력
.index('o') # 첫 o의 index
.lower() # 전부 소문자
.upper() # 전부 대문자

.rstrip() # 오른쪽 공백삭제
.lstrip() # 왼쪽 공백삭제
.strip() # 공백 삭제

.replace() # 바꾸기
.split() # 분리
.join() # 삽입
.append() # 끝에 추가
.extend([]) # 리스트에 리스트 추가
.insert() # 리스트에 요소(특정위치) 추가
.remove() # 리스트에서 요소 삭제

.count() # 갯수 세기

.sort() # 정렬
.reverse() # 순서 거꾸로

len() # 길이
type() # 데이터타입

del # 삭제

round(값, 자릿수)

.union() # 합집합
a.difference(b) # 차집합(a기준으로 b와 겹치지 않는 값)
a.intersection(b) # 교집합(a기준으로 b와 겹치는 값)

dic.keys() # 딕셔너리의 키 출력
dic.values() # 딕셔너리의 값 출력
dic.items() # 딕셔너리의 ([키, 값], [키, 값]...) 출력
dic.get('') # 키의 값 출력

abs() # 절대값
divmod() # (몫, 나머지)출력

all() # 입력값 모두 참 = True
any() # 입력값 모두 거짓 = False

chr() # 유니코드 숫자를 받아 해당문자 출력
ord() # 문자를 받아 해당 유니코드 숫자 출력

dir() # 객체가 가진 변수나 함수 알려줌
enumerate() # 인덱스와 값을 출력하거나 딕셔너리 키와 값을 출력할때 사용
sorted() # 순서대로 정렬
'''




